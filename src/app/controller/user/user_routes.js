const express = require('express');
const router = express.Router();
/**
const getPoint = require('./getPoint');
router.use('/user', getPoint);

const updatePoint = require('./updatePoint');
router.use('/user', updatePoint);

**/
module.exports = router;